---
phase: 085-probethium-synthesis
plan: 02
type: execute
wave: 2
depends_on: ["085-01"]
files_modified:
  - tests/probethium-synthesis.spec.js
autonomous: true

must_haves:
  truths:
    - "Tests confirm synthesis button only appears after probethium_synthesis research"
    - "Tests confirm conversion math: 5 exotics = 0.001 probethium per batch with remainders kept"
    - "Tests confirm button is disabled when exotic minerals < 5"
    - "Tests confirm animation queue processes multiple syntheses sequentially"
    - "Tests confirm synthesis provides probethium (resource actually increases)"
  artifacts:
    - path: "tests/probethium-synthesis.spec.js"
      provides: "Playwright tests for SYNTH-01 through SYNTH-04"
      min_lines: 80
  key_links:
    - from: "tests/probethium-synthesis.spec.js"
      to: "src/GameState.js"
      via: "evaluate() sets up research state and resources"
      pattern: "probethium_synthesis"
    - from: "tests/probethium-synthesis.spec.js"
      to: "src/DetailsPanel.js"
      via: "checks for #synthesizeBtn visibility and state"
      pattern: "synthesizeBtn"
---

<objective>
Write Playwright tests covering all probethium synthesis requirements (SYNTH-01 through SYNTH-04).

Purpose: Automated verification that synthesis research unlock, hub button, conversion logic, and animation queueing work correctly.
Output: tests/probethium-synthesis.spec.js with comprehensive test coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/085-probethium-synthesis/085-CONTEXT.md
@.planning/phases/085-probethium-synthesis/085-RESEARCH.md
@.planning/phases/085-probethium-synthesis/085-01-SUMMARY.md
@tests/shell-bonus-integration.spec.js
@tests/research-gating.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Playwright tests for probethium synthesis</name>
  <files>tests/probethium-synthesis.spec.js</files>
  <action>
Create `tests/probethium-synthesis.spec.js` with the following test cases. Follow existing test patterns: dismiss tutorial panel first, use `page.evaluate()` for game state setup, use `page.goto('http://localhost:8080')` to start.

**Test setup helper (beforeEach or shared function):**
```javascript
// Dismiss tutorial panel
await page.evaluate(() => {
    const tutorialPanel = document.getElementById('tutorialPanel');
    if (tutorialPanel) tutorialPanel.style.display = 'none';
});
```

**Test 1: SYNTH-02 -- Research node exists in alien tech tree**
- `page.evaluate()` to check `window.game.gameState.researchSystem.tree.probethium_synthesis` exists
- Verify properties: `id === 'probethium_synthesis'`, `cost === 3`, `parent === 'alien_tech'`, `tree === 'alien'`, `icon` includes alembic emoji
- Verify `alien_tech.children` includes `'probethium_synthesis'`

**Test 2: SYNTH-02 -- Synthesis button hidden when research not unlocked**
- Do NOT unlock `probethium_synthesis` research
- Select a hub via `page.evaluate()` (emit `entity:selected` with hub)
- Check that `#synthesizeBtn` does NOT exist in DOM (use `page.locator('#synthesizeBtn').count()` expecting 0)

**Test 3: SYNTH-01 -- Synthesis button appears after research unlock**
- `page.evaluate()` to set up:
  - `researchSystem.researched.add('probethium_synthesis')`
  - `researchSystem.tree.probethium_synthesis.researched = true`
  - `gameState.updateResources({ exoticMinerals: 10 })`
- Select a hub, emit `entity:selected`
- Wait for `#synthesizeBtn` to be visible
- Verify button contains text about "Synthesize"

**Test 4: SYNTH-01 -- Button disabled when exotic minerals < 5**
- Set up research unlocked but only 3 exotic minerals
- Select hub, check `#synthesizeBtn` exists
- Verify button is disabled (`await page.locator('#synthesizeBtn').isDisabled()` is true)
- Verify button text mentions insufficient resources or "Need"

**Test 5: SYNTH-01/SYNTH-04 -- Conversion math: single batch (5 exotics)**
- Set up: research unlocked, exactly 5 exotic minerals
- Record initial probethium: `gameState.probethium.current`
- Emit `synthesis:triggered` with hub reference
- Check resources after: exoticMinerals === 0, probethium increased by 0.001
- Verify probethium delta is exactly 0.001 (within floating point tolerance)

**Test 6: SYNTH-01/SYNTH-04 -- Conversion math: multiple batches with remainder**
- Set up: research unlocked, 12 exotic minerals
- Emit `synthesis:triggered` with hub
- Check resources: exoticMinerals === 2 (12 - 10 consumed), probethium increased by 0.002
- Verify 2 exotics remain (leftover from 12 / 5 = 2 batches, 2 remainder)

**Test 7: SYNTH-01 -- Insufficient exotics rejected**
- Set up: research unlocked, 3 exotic minerals
- Emit `synthesis:triggered` with hub
- Check resources: exoticMinerals still 3 (unchanged), probethium unchanged
- Synthesis should not proceed with < 5 exotics

**Test 8: SYNTH-03 -- Animation queue accepts synthesis events**
- Set up: research unlocked, 20 exotic minerals
- Emit `synthesis:triggered` 3 times in rapid succession (via page.evaluate loop)
- Check `window.game.synthesisAnimation` exists
- Check that queue has entries OR currentAnimation is active
- Queue + current animation count should be 3

**Test 9: SYNTH-03 -- Animation completes after 3 seconds**
- Set up: research unlocked, 5 exotic minerals
- Emit `synthesis:triggered`
- Verify `synthesisAnimation.currentAnimation` is not null (animation active)
- Wait 3500ms (3s animation + margin)
- Verify `synthesisAnimation.currentAnimation` is null (animation finished)
- Verify queue is empty

**Test 10: SYNTH-04 -- Probethium actually increases (viable source)**
- Set up: research unlocked, 50 exotic minerals, record initial probethium
- Emit `synthesis:triggered`
- Calculate expected: `Math.floor(50/5) * 0.001 = 0.010`
- Verify probethium increased by expected amount
- Verify this is a meaningful amount relative to starting probethium

Run all tests: `npx playwright test tests/probethium-synthesis.spec.js --project=chromium`
  </action>
  <verify>
```bash
npx playwright test tests/probethium-synthesis.spec.js --project=chromium
```
All tests pass. No flaky tests on second run.
  </verify>
  <done>
- tests/probethium-synthesis.spec.js exists with 10+ tests
- Tests cover SYNTH-01 (button visibility, conversion, exotic count display)
- Tests cover SYNTH-02 (research node properties, gating)
- Tests cover SYNTH-03 (animation queue, animation completion timing)
- Tests cover SYNTH-04 (viable probethium source, correct amounts)
- All tests pass on Chromium
  </done>
</task>

</tasks>

<verification>
1. `npx playwright test tests/probethium-synthesis.spec.js --project=chromium` -- all tests pass
2. `npx playwright test --project=chromium` -- full suite passes (no regressions)
3. Tests cover: research gating, button visibility, conversion math, edge cases, animation queue, probethium increase
</verification>

<success_criteria>
- 10+ Playwright tests covering all 4 SYNTH requirements
- All tests pass on Chromium
- No regressions in existing test suite
- Conversion math verified: 5 exotics = 0.001 probethium, remainders kept
- Research gating verified: button hidden without research, visible with research
- Animation queueing verified: multiple syntheses queue and complete
</success_criteria>

<output>
After completion, create `.planning/phases/085-probethium-synthesis/085-02-SUMMARY.md`
</output>
