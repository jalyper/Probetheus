---
phase: 10-testing-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/happy-path-integration.spec.js
autonomous: true

must_haves:
  truths:
    - "Full progression golden path works end-to-end: new game to probethium"
    - "Signal pipeline works across all 4 non-Standard sector types and Standard"
    - "Shell bonus stacking affects exclusive signal rarity and rewards"
    - "Economy pipeline from sector discovery to probethium synthesis works"
    - "Complete game state persists through save/load cycle"
  artifacts:
    - path: "tests/happy-path-integration.spec.js"
      provides: "End-to-end player flow integration tests"
      min_lines: 300
  key_links:
    - from: "tests/happy-path-integration.spec.js"
      to: "game.js, src/GameController.js, src/ProbeManager.js, src/SectorManager.js, src/SaveManager.js, src/ShellSystem.js"
      via: "page.evaluate() cross-system state manipulation and verification"
      pattern: "page\\.evaluate"
---

<objective>
Write the `tests/happy-path-integration.spec.js` integration test file covering 8-12 end-to-end player flow tests.

Purpose: Verify that multi-system features work together in realistic player scenarios. These are the "golden path" tests — full flows that cross 3+ game systems, not individual gate checks.

Output: `tests/happy-path-integration.spec.js` with 8-12 passing Playwright tests covering the golden path progression, signal pipeline, shell bonus stacking, economy pipeline, and save/load round-trip.
</objective>

<execution_context>
@~/.claude/agents/gsd-executor.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-testing-integration/10-CONTEXT.md
@.planning/phases/10-testing-integration/10-RESEARCH.md

Reference these existing tests for patterns:
@tests/discovery-reveal.spec.js
@tests/signal-rewards.spec.js
@tests/save-system.spec.js
@tests/shell-bonus-integration.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create happy-path-integration.spec.js with 8-12 flow tests</name>
  <files>tests/happy-path-integration.spec.js</files>
  <action>
Create `tests/happy-path-integration.spec.js` with the following structure. Use `const { test, expect } = require('@playwright/test');` format.

Include the standard startGame helper and beforeEach pattern (see exclusive-signals.spec.js for exact pattern).

Include a helper function `createTestSector(page, typeName, x, y, profileType)` that programmatically creates sectors with proper type definitions including exclusiveSignalType, resourceProfile, and all required fields (see discovery-reveal.spec.js triggerSectorDiscovery helper for the sector type definitions: Standard, Resource-Rich, Data Haven, Ancient, Asteroid Field).

Write the following test groups:

**1. Golden Path Progression (2-3 tests):**

- `golden path: new game to research unlock` — Start new game. Verify initial state (0 resources, 3 probes). Programmatically give 100 minerals and 1 research point. Build second hub (add hub to entities, emit hub:built). Set tutorial gate (researchAccessAllowed = true). Verify research unlocks. Research auto_minerals (1 RP). Verify probe can now equip mineral collector.

- `golden path: research to mining infrastructure` — Start game. Programmatically set up mid-game state: 200 minerals, 100 data, research unlocked, auto_minerals researched. Install mineral collector on probe (25M cost). Verify probe has equipment. Build mining station (100M + 50D). Build shuttle (50M + 25D). Verify mining infrastructure exists and probethium production state is ready.

- `golden path: mining to probethium synthesis` — Start game. Set up late-game state: mining station with sector profile (probethium-rich), shuttle connected, alien_tech and probethium_synthesis researched, 10 exotic minerals. Call synthesis at hub. Verify probethium increases by 0.002 (2 batches of 5 exotics). Verify 0 exotic minerals remain.

**2. Signal Pipeline (3-4 tests):**

- `signal pipeline: Resource-Rich sector exclusive signal reward` — Create Resource-Rich sector. Spawn discovery bonus signals. Verify an ore_vein signal exists with epic or legendary rarity. Set up a probe with empty cargo. Simulate exploration of the ore_vein signal via gameController.explore('excavate'). Verify 2x mineral yield (compare against standard mineral signal yield). Crosses: SectorManager -> signal spawning -> GameController rewards.

- `signal pipeline: all 4 non-Standard sectors spawn correct exclusive type` — For each of Resource-Rich, Data Haven, Ancient, Asteroid Field: create sector, call spawnDiscoveryBonusSignals(), verify the correct exclusive signal type appears (ore_vein, data_cache, relic, exotic_crystal respectively). Verify Standard sector does NOT spawn any exclusive. This is a multi-sector integration check, not a unit check.

- `signal pipeline: discovery modal shows correct info then signals spawn` — Create Resource-Rich sector. Trigger showSectorDiscovery. Verify modal content contains "Ore Vein" and sector resource profile info. Click Continue button. Verify signals were spawned in the game world. Crosses: SectorManager discovery -> UI modal -> signal spawning.

- `signal pipeline: Exotic Crystal yields exotic minerals for synthesis chain` — Create Asteroid Field sector. Spawn exotic_crystal signal. Simulate probe exploration with deterministic Math.random (force 60% exotic path). Verify probe.cargo.exoticMinerals > 0. Then: set up synthesis research and use hub to synthesize. Verify full chain: signal -> exotic minerals -> probethium. Crosses: signal -> rewards -> synthesis.

**3. Shell Bonus Stacking (1-2 tests):**

- `shell bonus: rareSignalChance affects exclusive signal rarity distribution` — Find a shell with rareSignalChance bonus in SHELL_CATALOG.probes. Equip it on probe. Generate 500 exclusive signals in a Resource-Rich sector with the bonused probe vs without. Compare common-rarity rates: bonused probe should have lower common rate. Crosses: ShellSystem -> ProbeManager signal generation.

- `shell bonus: explorationRewards boosts exclusive signal rewards` — Equip shell with explorationRewards bonus on probe. Override Math.random for deterministic test. Explore an ore_vein signal with the bonused probe. Compare mineral yield against unbonused baseline. Verify bonused yield > baseline. Crosses: ShellSystem -> GameController rewards.

**4. Save/Load Round-Trip (2 tests):**

- `save/load preserves complete mid-game state` — Start game. Set up complex state: minerals=500, data=300, 2 hubs, research unlocked with auto_minerals + auto_data researched, 5 research points, probe with mineral_collector equipped, explored sectors with resource profiles. Save via saveManager.saveGame(1). Mutate state (set minerals=0, clear research). Load via saveManager.loadGame(1). Wait 500ms. Verify ALL state restored: resources, research, equipment, sectors.

- `save/load preserves sector resource profiles and exclusive signal types` — Create sectors of each type (Resource-Rich, Data Haven, Ancient, Asteroid Field) with specific resource profiles. Save. Load. Verify each sector still has correct type.exclusiveSignalType AND correct resourceProfile.type. Crosses: SaveManager -> SectorManager -> sector persistence.

IMPORTANT IMPLEMENTATION NOTES:
- These are INTEGRATION tests — each test should cross 3+ game systems.
- Use page.evaluate() for state setup and verification.
- For reward tests, override Math.random for determinism (see signal-rewards.spec.js pattern).
- Always restore Math.random in a finally-like pattern.
- Programmatic save/load via saveManager.saveGame(1) / saveManager.loadGame(1) (not UI).
- After loadGame, wait 500ms for state to settle.
- Include resourceProfile on all programmatic sectors.
- Equipment is array format: `probe.equipment = [{ type: 'mineral_collector', ... }]`.
- For shell bonus tests, use shellSystem.getEntityBonus() to verify bonus is applied.
- For synthesis, the conversion is: Math.floor(exoticMinerals / 5) * 0.001.

Target: 8-12 tests. Each test should be a meaningful end-to-end flow, not a unit check.
  </action>
  <verify>
Run: `npx playwright test tests/happy-path-integration.spec.js --project=chromium`
All tests pass. Test count is 8-12.
  </verify>
  <done>
happy-path-integration.spec.js exists with 8-12 passing tests covering golden path progression, signal pipeline, shell bonus stacking, and save/load round-trip. Each test crosses 3+ game systems.
  </done>
</task>

</tasks>

<verification>
1. `npx playwright test tests/happy-path-integration.spec.js --project=chromium` — all tests pass
2. Test count is between 8 and 12
3. Tests cover all 5 flow categories defined in CONTEXT.md section 3
4. Each test crosses 3+ game systems (not unit-level)
5. Save/load tests use programmatic API (not UI clicking)
</verification>

<success_criteria>
- 8-12 Playwright tests passing in happy-path-integration.spec.js
- Golden path, signal pipeline, shell bonus, and save/load flows all covered
- Tests use cross-system verification (checking multiple game systems per test)
- Math.random is overridden and restored for deterministic reward tests
- No test takes longer than 30 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/10-testing-integration/10-02-SUMMARY.md`
</output>
