---
phase: 04-bonus-ui-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/DetailsPanel.js
autonomous: true

must_haves:
  truths:
    - "Hub detail panel shows the currently equipped hub shell with name and preview"
    - "Mining station detail panel shows the currently equipped station shell with name and preview"
    - "Hovering the equipped shell indicator in the hub detail panel shows a bonus tooltip"
    - "Hovering the equipped shell indicator in the mining station detail panel shows a bonus tooltip"
    - "If no shell is equipped (default), the indicator shows 'Default' with no tooltip"
  artifacts:
    - path: "src/DetailsPanel.js"
      provides: "Shell indicator sections in hub and mining station detail panels with tooltip hover"
      contains: "shellIndicator"
  key_links:
    - from: "src/DetailsPanel.js (hub panel)"
      to: "src/ShellSystem.js (getEquippedShell)"
      via: "Reads equipped hub shell to display indicator"
      pattern: "getEquippedShell.*hubs"
    - from: "src/DetailsPanel.js (station panel)"
      to: "src/ShellSystem.js (getEquippedShell)"
      via: "Reads equipped station shell to display indicator"
      pattern: "getEquippedShell.*miningStations"
    - from: "src/DetailsPanel.js"
      to: "src/UIManager.js (attachTooltipHandlers)"
      via: "Calls UIManager tooltip system for hover behavior"
      pattern: "attachTooltipHandlers"
---

<objective>
Add equipped shell indicator sections to the hub and mining station detail panels in DetailsPanel.js. Each indicator shows the shell name, a small color swatch, and supports hover tooltips via the UIManager tooltip system (created in Plan 01, but the calls can be added now -- they will work once Plan 01 is also complete since both are Wave 1).

Purpose: Players can see what shell is equipped on hubs and mining stations, and hover to see bonus effects (BUI-03, BUI-04).
Output: Shell indicator sections in hub and mining station detail panels.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-bonus-ui-integration/04-CONTEXT.md
@.planning/phases/04-bonus-ui-integration/04-RESEARCH.md
@src/DetailsPanel.js
@src/ShellSystem.js
@src/UIManager.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shell indicator to hub detail panel</name>
  <files>src/DetailsPanel.js</files>
  <action>
In `showHubDetails(hub)`, after the Hub Operations section (before the closing backtick of `this.content.innerHTML`), add a new section for the equipped shell:

1. Get the shell system: `const shellSystem = window.game?.shellSystem;`
2. Get equipped hub shell: `const equippedShellId = this.gameState.cosmetics?.equippedShells?.hubs || 'default';`
3. Look up shell data: `const shell = shellSystem ? shellSystem.getShellById('hubs', equippedShellId) : null;`
   - Note: `getShellById` may not exist. Check ShellSystem.js for the correct method. It might be through SHELL_CATALOG: `window.SHELL_CATALOG?.hubs?.[equippedShellId]`. Use whichever approach works.
4. Determine shell color: `const shellColor = shell?.visual?.color || '#0f8';`
5. Add this HTML section after the Hub Operations div:

```html
<div style="background: rgba(148,0,211,0.05); border: 1px solid rgba(148,0,211,0.2); border-radius: 5px; padding: 10px; margin-top: 12px;">
    <div style="color: #9400d3; font-size: 12px; font-weight: bold; margin-bottom: 6px;">🎨 Equipped Shell</div>
    <div id="hubShellIndicator" style="display: flex; align-items: center; gap: 8px; cursor: default;">
        <div style="width: 24px; height: 24px; border-radius: 4px; background: ${shellColor}; border: 1px solid rgba(255,255,255,0.2);"></div>
        <div>
            <div style="color: ${shellColor}; font-size: 11px; font-weight: bold;">${shell?.name || 'Default'}</div>
            <div style="color: #888; font-size: 9px; text-transform: uppercase;">${shell?.rarity || 'standard'}</div>
        </div>
    </div>
</div>
```

6. After `this.setupHubButtons(hub)` is called (which happens after innerHTML is set), add tooltip attachment:
```javascript
if (shell && shell.bonuses && Object.keys(shell.bonuses).length > 0) {
    const indicator = document.getElementById('hubShellIndicator');
    if (indicator && window.game?.uiManager?.attachTooltipHandlers) {
        window.game.uiManager.attachTooltipHandlers(indicator, shell);
    }
}
```

This requires the innerHTML to be set BEFORE attaching event handlers, which is already the pattern used (setupHubButtons runs after innerHTML assignment).
  </action>
  <verify>
Run game, click on a hub, verify the shell indicator section appears showing "Default" or the equipped shell name with a color swatch. If a shell with bonuses is equipped, hover to see tooltip.
  </verify>
  <done>
Hub detail panel shows equipped shell indicator with name, color swatch, and rarity. Hovering the indicator shows bonus tooltip if the shell has bonuses.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add shell indicator to mining station detail panel</name>
  <files>src/DetailsPanel.js</files>
  <action>
In `showMiningStationDetails(station)`, after the upgrade button section and before the status footer div (the one with "Station needs resources..." text), add a shell indicator section:

1. Get shell system and equipped shell (same pattern as hub):
   - `const shellSystem = window.game?.shellSystem;`
   - `const equippedShellId = this.gameState.cosmetics?.equippedShells?.miningStations || 'default';`
   - `const stationShell = shellSystem ? (window.SHELL_CATALOG?.miningStations?.[equippedShellId] || null) : null;`
   - `const stationShellColor = stationShell?.visual?.color || '#c9f';`

2. Add this HTML to the innerHTML template, after the upgrade button div and before the final status div:

```html
<div style="background: rgba(148,0,211,0.05); border: 1px solid rgba(148,0,211,0.2); border-radius: 5px; padding: 10px; margin-top: 12px;">
    <div style="color: #9400d3; font-size: 12px; font-weight: bold; margin-bottom: 6px;">🎨 Equipped Shell</div>
    <div id="stationShellIndicator" style="display: flex; align-items: center; gap: 8px; cursor: default;">
        <div style="width: 24px; height: 24px; border-radius: 4px; background: ${stationShellColor}; border: 1px solid rgba(255,255,255,0.2);"></div>
        <div>
            <div style="color: ${stationShellColor}; font-size: 11px; font-weight: bold;">${stationShell?.name || 'Default'}</div>
            <div style="color: #888; font-size: 9px; text-transform: uppercase;">${stationShell?.rarity || 'standard'}</div>
        </div>
    </div>
</div>
```

3. After `this.setupMiningStationButtons(station)` and `this.updateMiningStationButtonStates(station)`, add tooltip attachment:
```javascript
if (stationShell && stationShell.bonuses && Object.keys(stationShell.bonuses).length > 0) {
    const indicator = document.getElementById('stationShellIndicator');
    if (indicator && window.game?.uiManager?.attachTooltipHandlers) {
        window.game.uiManager.attachTooltipHandlers(indicator, stationShell);
    }
}
```

Note: The shell variables (`stationShell`, `stationShellColor`) must be declared BEFORE the template literal, alongside the other variables at the top of the method. Move the shell lookup code to just after the existing variable declarations (after `const connectedShuttles = ...`).
  </action>
  <verify>
Run game, build a mining station, click on it, verify the shell indicator section appears showing "Default" or the equipped shell. If a shell with bonuses is equipped, hover to see tooltip.
  </verify>
  <done>
Mining station detail panel shows equipped shell indicator with name, color swatch, and rarity. Hovering the indicator shows bonus tooltip if the shell has bonuses.
  </done>
</task>

</tasks>

<verification>
1. Open game, click on a hub -- shell indicator section visible with "Default" or equipped shell
2. Open game, click on a mining station -- shell indicator section visible
3. If shells with bonuses are equipped, hover the indicator -- tooltip appears
4. If default shell (no bonuses), hover the indicator -- no tooltip
5. Both panels still render correctly with all existing content intact
</verification>

<success_criteria>
- BUI-03: Hub detail panel shows bonus tooltip on hover -- DONE
- BUI-04: Mining station detail panel shows bonus tooltip on hover -- DONE
- Shell indicators show name, color swatch, and rarity for equipped shells
- Tooltip integration with UIManager.attachTooltipHandlers works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-bonus-ui-integration/04-02-SUMMARY.md`
</output>
