---
phase: 02-shell-persistence-on-save-load
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/SaveManager.js
  - tests/shell-persistence.spec.js
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Probe shellId persists through save/load cycle"
    - "Probe visual appearance (color, trail, glow) is restored after load"
    - "Multiple probes with different shells retain their individual shells"
  artifacts:
    - path: "src/SaveManager.js"
      provides: "shellId serialization and cosmetic refresh on load"
      contains: "shellId"
    - path: "tests/shell-persistence.spec.js"
      provides: "Integration test for shell persistence"
      min_lines: 50
  key_links:
    - from: "src/SaveManager.js createSaveData()"
      to: "probe.shellId"
      via: "serialization in probe mapping"
      pattern: "shellId.*probe\\.shellId"
    - from: "src/SaveManager.js restoreFromData()"
      to: "ShellSystem.refreshProbeCosmetic()"
      via: "forEach loop after probe restoration"
      pattern: "refreshProbeCosmetic"
---

<objective>
Fix shell persistence gap: per-probe shellId is not saved/loaded, causing shell visual loss on reload.

Purpose: Close the critical gap identified in v1.1-MILESTONE-AUDIT.md where equipped shells are lost when saving and loading the game.

Output: SaveManager.js modifications + integration test proving shells persist through save/load.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.1-MILESTONE-AUDIT.md
@src/SaveManager.js
@src/ShellSystem.js
@tests/shell-visuals.spec.js
@tests/save-system.spec.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shellId serialization to SaveManager</name>
  <files>src/SaveManager.js</files>
  <action>
In SaveManager.createSaveData(), find the probe serialization block (lines ~99-116).
Add shellId to the probe object mapping:

```javascript
probes: this.gameState.entities.probes.map(probe => ({
    id: probe.id,
    // ... existing fields ...
    shellId: probe.shellId || 'default',  // ADD THIS LINE
    returnSpeed: probe.returnSpeed || 0.0003
})),
```

In SaveManager.restoreFromData(), find the probe restoration block (lines ~487-530).
After the probe map completes but BEFORE `return probe;`, add cosmetic refresh:

```javascript
// Inside the probe map, after equipment migration but before return probe:
// Restore shellId (already comes from probeData)
// cosmetic will be refreshed after all probes are restored
```

Then, AFTER the probe array is fully restored (after line ~530), add:

```javascript
// Refresh probe cosmetics from shellIds
if (window.game?.shellSystem) {
    this.gameState.entities.probes.forEach(probe => {
        window.game.shellSystem.refreshProbeCosmetic(probe);
    });
    console.log('Shell cosmetics refreshed for', this.gameState.entities.probes.length, 'probes');
}
```

This must happen AFTER all probes are restored so the probes array is fully populated.
  </action>
  <verify>
Run grep to confirm shellId appears in createSaveData:
`grep -n "shellId" src/SaveManager.js`

Should show shellId in both createSaveData and restoreFromData sections.
  </verify>
  <done>
SaveManager.js includes shellId in probe serialization and calls refreshProbeCosmetic() after restoring probes from save data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create shell persistence integration test</name>
  <files>tests/shell-persistence.spec.js</files>
  <action>
Create new test file `tests/shell-persistence.spec.js` following the patterns from save-system.spec.js and shell-visuals.spec.js.

Test cases to implement:

1. **Single probe shell persists through save/load**
   - Start game, equip solar_flare shell on first probe
   - Save game programmatically via saveManager.saveGame('slot1')
   - Reload page, load game via saveManager.loadGame('slot1')
   - Verify probe.shellId === 'solar_flare'
   - Verify probe.cosmetic.bodyColor === '#ff4500'
   - Verify probe.cosmetic.glow === true

2. **Multiple probes with different shells persist**
   - Start game with 3 probes (deploy 2 more from hub)
   - Equip different shells on each: default, void_walker, solar_flare
   - Save and reload
   - Verify each probe retained its specific shell
   - Verify each probe has correct cosmetic colors

3. **Probe without shellId gets default cosmetic after load**
   - Start game (probes have no shellId initially)
   - Save and reload
   - Verify probes have cosmetic set (cyan default color)
   - Verify probe.cosmetic.glow === false

Key patterns to use:
- Use page.evaluate() to access window.game.saveManager directly
- Use page.waitForFunction() to wait for systems to be ready
- Clear localStorage in beforeEach
- Dismiss tutorial panel to prevent blocking
  </action>
  <verify>
Run the new tests:
`npx playwright test tests/shell-persistence.spec.js --project=chromium`

All tests should pass.
  </verify>
  <done>
tests/shell-persistence.spec.js exists with 3+ tests covering single shell persistence, multiple shells, and default shell handling. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite to verify no regressions</name>
  <files></files>
  <action>
Run the complete test suite to ensure the SaveManager changes don't break existing functionality:

`npx playwright test --project=chromium`

Pay particular attention to:
- save-system.spec.js (save/load core functionality)
- shell-visuals.spec.js (shell cosmetic application)
- equipment-slots.spec.js (equipment persistence uses similar patterns)

If any tests fail, investigate and fix the regression.
  </action>
  <verify>
All existing tests pass. No regressions introduced.
  </verify>
  <done>
Full test suite passes with 0 failures. Shell persistence is complete and integrated.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Manual verification (optional but recommended):
   - Start new game
   - Open probe details, equip a non-default shell (e.g., via Dark Market or direct gameState manipulation)
   - Save game to slot
   - Reload browser
   - Load game from slot
   - Open probe details - should show the same shell equipped with correct visual

2. Code verification:
   - `grep -n "shellId" src/SaveManager.js` shows shellId in createSaveData and restoreFromData
   - `grep -n "refreshProbeCosmetic" src/SaveManager.js` shows the call after probe restoration
   - All tests pass: `npx playwright test --project=chromium`
</verification>

<success_criteria>
- probe.shellId is serialized in SaveManager.createSaveData()
- probe.shellId is restored from save data
- refreshProbeCosmetic() is called after all probes are restored
- Integration test proves shells survive save/load cycle
- No regressions in existing tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-shell-persistence-on-save-load/02-01-SUMMARY.md`
</output>
